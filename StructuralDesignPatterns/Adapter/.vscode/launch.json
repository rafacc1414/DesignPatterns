{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "cppdbg",
            "stopAtEntry": true,
            "request": "launch",
            "program": "${workspaceFolder}/debug/Adapter.bin",
            "cwd": "${workspaceRoot}",
            "MIMode": "gdb"
        }
    ]
}